<br/>
<br/>
<div class="row">
    <div class="col-xs-12">
      {{#each novelTokens}}
          <span class="formatted-novel-token">{{content}}</span>
      {{/each}}
      {{view 'live-input'}}
    </div>
</div>

{{outlet}}
